---
Runtime:
  LogToStdout: true

Jobs:
  - Id: 'Job1'
    Run: 'echo hey'
    CaptureOutput: true
    DependsOn: []

  - Id: 'job2'
    Run: 'for i in $(seq 1 10); do sleep 0.2; echo "{{ .Jobs.Job1.Output }}" ; done'
    DependsOn: [ 'Job1' ]

  - Id: 'job3'
    Run: 'for i in $(seq 1 10); do sleep 0.2; echo ---------- ; done'
    DependsOn: [ 'Job1' ]
